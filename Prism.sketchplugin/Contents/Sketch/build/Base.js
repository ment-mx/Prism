// Generated by CoffeeScript 1.10.0
var Base;

Base = (function() {
  function Base(context, layer1) {
    this.context = context;
    this.layer = layer1 != null ? layer1 : null;
    this.command = this.context.command;
    this.pluginID = this.context.plugin.identifier();
    this.template = new Template(this.context.plugin.url());
    this.currentPage = this.layer ? (log("SJAHDKAJSHD"), this.layer.parentPage()) : this.context.document ? this.context.document.currentPage() : void 0;
  }

  Base.prototype.setValue_forKey = function(value, key) {
    return this.setValue_forKey_onLayer(value, key, this.layer);
  };

  Base.prototype.valueForKey = function(key) {
    return this.valueForKey_onLayer(key, this.layer);
  };

  Base.prototype.setValue_forKey_onLayer = function(value, key, layer) {
    return this.command.setValue_forKey_onLayer_forPluginIdentifier(value, key, layer, this.pluginID);
  };

  Base.prototype.valueForKey_onLayer = function(key, layer) {
    return this.command.valueForKey_onLayer_forPluginIdentifier(key, layer, this.pluginID);
  };

  return Base;

})();
